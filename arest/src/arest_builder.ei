import { RoutingBuilder } from "routing"
import { McpBuilder } from ".mcp.mcp_builder"

type ArestBuilder(
    val port: Int,
    var running: Bool = true,
    val routingBuilder: RoutingBuilder = RoutingBuilder(),
    val mcpBuilder: McpBuilder = McpBuilder()
) {

    fun routing(init: RoutingBuilder.() -> Void) {
        init(routingBuilder)
    }

    fun mcp(init: McpBuilder.() -> Void) {
        init(mcpBuilder)
    }

    fun shutdown() {
        running = false
    }

}